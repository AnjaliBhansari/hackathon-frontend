import React from 'react';
import { HomePage } from '@/presentation/features/home';

/**
 * Next.js index page that serves as the entry point for the home route.
 * This page uses the HomePage component from our presentation layer.
 */
export default function Home() {
  return <HomePage />;
} 